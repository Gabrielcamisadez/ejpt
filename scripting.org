#+TITLE: 🛠️ Shell Scripting Essentials for CyberSec
#+AUTHOR: Gabriel

* 🚀 1. VARIÁVEIS E SUBSTITUIÇÃO
Capturando dados dinâmicos do sistema.
#+BEGIN_SRC bash
# 📝 Variável simples
TOOL_NAME="NetScanner"

# 💻 Captura de comando (Command Substitution)
CURRENT_IP=$(hostname -I | awk '{print $1}')
OS_VERSION=$(cat /etc/os-release | grep "PRETTY_NAME" | cut -d'"' -f2)

echo "🏷️ Nome: $TOOL_NAME | 🌐 IP: $CURRENT_IP | 🐧 OS: $OS_VERSION"
#+END_SRC

* 🚦 2. CONDICIONAIS (IF/ELSE)
Lógica de validação e segurança.
#+BEGIN_SRC bash
# 🔍 Checando se o usuário é root (UID 0)
if [ "$EUID" -ne 0 ]; then
    echo "❌ Erro: Este script precisa de privilégios root."
    exit 1
else
    echo "✅ Acesso concedido. Iniciando análise..."
fi
#+END_SRC

* 🔄 3. LOOPS E PROCESSAMENTO
Iterando sobre alvos ou saídas de comandos.
#+BEGIN_SRC bash
# 🌀 Loop FOR (Lista de alvos)
for ip in 192.168.1.{1..5}; do
    echo "📡 Pingando host: $ip"
done

# 📥 Loop WHILE (Processando o SS linha por linha)
ss -nltp | tail -n +2 | while read -r line; do
    echo "⛓️  Conexão bruta: $line"
done
#+END_SRC

* ✂️ 4. MANIPULAÇÃO DE TEXTO (PARSING)
A mágica para limpar o 'ss -nltp'.
#+BEGIN_SRC bash
# 🧪 Exemplo: Extraindo a porta de '0.0.0.0:443'
RAW_DATA="0.0.0.0:443"

# awk '{print $NF}' pega a última parte após o delimitador
PORT=$(echo $RAW_DATA | awk -F':' '{print $NF}')

echo "🔌 Porta identificada: $PORT"
#+END_SRC

* 📥 5. ARGUMENTOS DE ENTRADA
Scripts dinâmicos via linha de comando.
#+BEGIN_SRC bash
# 🚩 $1 = primeiro parâmetro, $2 = segundo...
TARGET_IP=$1
PORT_RANGE=$2

echo "🎯 Alvo: ${TARGET_IP:-"Nenhum definido"} | 🔢 Portas: ${PORT_RANGE:-"Todas"}"
#+END_SRC

* 📁 6. REDIRECIONAMENTO E SILÊNCIO
Gestão de logs e erros.
#+BEGIN_SRC bash
# 📓 > (Novo log) | >> (Adicionar ao log) | 2> /dev/null (Mudo)
echo "--- Relatório de Scan ---" > report.log
nmap -sS -p 80 $TARGET_IP >> report.log 2> /dev/null
#+END_SRC

* 🎨 7. ESTÉTICA (CORES ANSI)
#+BEGIN_SRC bash
RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m' # No Color

echo -e "${GREEN}[+] Serviço Ativo${NC}"
echo -e "${RED}[!] Vulnerabilidade Crítica${NC}"
#+END_SRC
